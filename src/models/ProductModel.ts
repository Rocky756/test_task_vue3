export default interface ProductModel {
  name: string;
  maxSupply: number;
  category: string;
  visibility: boolean;
  fragile: boolean;
  perishable: boolean;
  heavyWeight: boolean;
  id: string,
};
